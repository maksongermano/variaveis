let idade = 31;
console.log(idade);
// camelCase é o padrão de escrita que separa as palavras de uma variável com letras maiusculas;

let alturaEmMetros = 1.83;
console.log(alturaEmMetros);

// String Template (no lugar de '' para escrever o que quer que exiba, podemos usar crase, assim podemos rodar Javascript e substituir campos por variáveis);

console.log(`Minha Idade é ${idade} anos e minha altura é ${alturaEmMetros} metros`);
